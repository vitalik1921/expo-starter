import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native-ui-lib";

export const Tab2 = () => {
  return (
    <SafeAreaView>
      <Text>Tab2</Text>
    </SafeAreaView>
  );
};

export default Tab2;
