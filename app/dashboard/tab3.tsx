import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native-ui-lib";

export const Tab3 = () => {
  return (
    <SafeAreaView>
      <Text>Tab3</Text>
    </SafeAreaView>
  );
};

export default Tab3;
